---
name: 'ks-100'
description: 用户的「满血执行标准」——对复杂任务以最大严谨度做深度地毯式执行：先侦察，再按可分维度大胆使用当前宿主的协作 agent 并行，主线程复核高风险结论，交付前逐项自查未覆盖项。用户显式调用 `$ks-100`，或明确要求毫无遗漏、满血模式、深度地毯式、派足够多 agent、全维度不遗漏时使用；普通的“详细一点”不触发。
---

# 满血执行标准

对用户当前任务做毫无遗漏、具体、清晰、细致的深度执行。

## 执行标准

1. **先侦察，再编排。** 主线程先建立事实底座、作用域、约束和 work list，再决定哪些部分可独立并行。
2. **大胆但有边界地并行。** 用户已授权 agent 并行时，尽量占满可用并发槽；每个 agent 必须有互不重叠、可独立验收的明确任务。共享配置和最终集成只保留一个 writer。
3. **遵守当前宿主的模型与派发规则。** 默认继承当前会话模型与推理强度；只有用户、项目规则或当前技能明确要求时才覆盖。Claude Code 使用实际可用的 Agent／Workflow 接口，Codex 使用 collaboration 接口；模型名、推理档位和 fork 参数按宿主能力核实，不把一端的参数直接用于另一端，不承诺不存在的 per-agent effort。
4. **高风险结论亲自复核。** 对最反直觉、影响最大、agent 之间矛盾或涉及安全/凭据/破坏性动作的结论，主线程必须回到原始证据做独立核验；发现自己判断错误时透明纠正。
5. **按任务类型设最低验收线。** 研究任务要求来源交叉验证和未证实项；代码任务要求构造真实分支与边界场景做行为级验证并独立审查，语法检查不能代替行为验证；标的深调／决策卡要求取数、终审、独立红队与复审，但不把这套流程硬套到其他任务；迁移任务要求备份、作用域保持、结构校验、运行时冒烟测试和回滚检查。
6. **交付前逐项补漏。** 对照 work list 标记已完成、不适用、阻塞或需用户授权。最终明确列出仍未覆盖的项目及原因，不用模糊措辞假装完成。
7. **客观优先。** 不迎合用户或材料的预设；证据不足就写“无法证实”或“这是推断”，不为了看起来完整而编造。
8. **首次就使用足够强的编制。** 如果后续发现某类任务需要更高的验证标准，要回溯检查此前同类产物是否也需要补跑，而不是只增强最后一项。

一句话流程：侦察 → 最强合适编制 → 并行执行 → 主线程复核 → 按任务类型验收 → 自查补漏 → 客观收口。
